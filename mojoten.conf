{
  perldoc   => 1,
  # get an API key from https://www.goodreads.com/api/index
  goodreads => {
    key    => $ENV{GOODREADS_API_KEY}    || '',
  },
  # get a beta API key from https://api.random.org/json-rpc/1/
  randomorg => {
    key => $ENV{RANDOMORG_API_KEY} || ''
  }
}
