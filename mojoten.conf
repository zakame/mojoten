{
  perldoc   => 1,
  goodreads => {
    key    => $ENV{GOODREADS_API_KEY}    || '',
    secret => $ENV{GOODREADS_API_SECRET} || ''
  },
  randomorg => {
    key => $ENV{RANDOMORG_API_KEY} || ''
  }
}
